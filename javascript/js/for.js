'use strict'

//Bucle For = una estructura de control que se repite varias veces

var numero = 100

for (var i = 0 ; i <= numero; i++) { //++ le suma uno al contador
    console.log("El numero es: " + i);

    debugger; // para poder debuggear 
}